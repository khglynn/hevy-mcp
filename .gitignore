# Secrets — NEVER commit
.dev.vars
.dev.vars.*
.env
.env.*

# Dependencies / build
node_modules/
dist/
.wrangler/

# Logs / OS
*.log
.DS_Store

# KV snapshots taken at cutover (hold encrypted grants; never commit)
backups/
